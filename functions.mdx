---
title: "Functions"
icon: "function"
---

Lets talk about functions. Its fundamental part of any programming language. And Flame supports them!

```shellscript
func add(i32 a, i32 b) i32 {
    return a+b;
}
```

Function declaration starts with `func` keyword and followed by function name, in our example it is `add` . Then we sta